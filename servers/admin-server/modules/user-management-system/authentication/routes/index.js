/**
 * @fileoverview Authentication Routes Barrel Export
 * @module servers/admin-server/modules/user-management-system/authentication/routes
 * @description Centralized export point for all authentication routes
 * @version 1.0.0
 */

'use strict';

const AuthRoutes = require('./auth-routes');

module.exports = {
  AuthRoutes
};
